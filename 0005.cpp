#include <iostream>
#include <cmath>
#include <stdio.h>
using namespace std;
int main() {
    double a, b;
    cin >> a >> b;
    //cout << pow(a*a+b*b,0.5);
    printf("%f", pow(a * a + b * b, 0.5));
}